# BlueskyCrawler
A Bluesky post crawler which finds relevant conspiracy theories and paranormal encounters to a user's query as defined.

## Quick Set-Up
The original clean data used to build the index is provided in the repository. The first step is to build the index using this data by the following command:
```bash
./index.sh